"""kohaku — Python reference implementation of Kohaku Protocol v0.1.

Backend for generating and serving declarative UI Specs (data). The single source of the contract is
the repository's spec/ (SPEC.md + JSON Schema); wire-compatible with the TS reference implementation
(byte-identical canonical JSON).
"""

__version__ = "0.2.0"
