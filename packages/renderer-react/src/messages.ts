// Catalog of UI messages the renderer uses for display. The framework-free "source of truth" is @kohaku-ui/renderer-core.
// renderer-react re-exports it directly instead of keeping a self-contained copy (public API and values unchanged).
export { DEFAULT_MESSAGES, type RendererMessages } from "@kohaku-ui/renderer-core";
