{
  "name": "@kohaku-ui/port-contracts",
  "version": "0.2.0",
  "private": true,
  "description": "Vitest contract suites for kohaku's StoragePort and AuthzPort. Test-only, never published: every adapter (memory / file / redis / postgres / hmac / jwt) runs the same suite.",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit" },
  "dependencies": { "@kohaku-ui/spec-core": "workspace:^" },
  "peerDependencies": { "vitest": "catalog:" },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "license": "Apache-2.0"
}
